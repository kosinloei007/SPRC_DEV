# Create Stored Procedure: `usp_cfg_get_file_info_20290606`

## Parameters

| ชื่อ | ชนิด |
| --- | --- |
| `@process_code` | `NVARCHAR(5)` |
| `@process_key` | `NVARCHAR(100)` |

## Requirement (initial)

SELECT ข้อมูลทุก field จากตาราง `dbo.cfg_file_information`.
